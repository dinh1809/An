# 🚀 Antigravity Expert Skills - Guide Guide

Tài liệu này tổng hợp tất cả các kỹ năng chuyên gia đã được cài đặt trong dự án của bạn. Các kỹ năng này hoạt động dưới dạng **"Auto-apply"** — nghĩa là tôi sẽ tự động kích hoạt chúng khi nhận thấy yêu cầu của bạn thuộc lĩnh vực chuyên môn tương ứng.

---

## 🛠️ Danh sách Skill Chuyên Gia

Dưới đây là 36 kỹ năng hiện có, được chia theo nhóm chuyên môn:

### 1. Frontend & UI/UX
| Tên Skill | Khi nào sử dụng |
| :--- | :--- |
| `ui-ux-pro-max` | Thiết kế giao diện cao cấp, bảng màu, font chữ, layout (SaaS, Dashboard, Mobile). |
| `react-expert` | Xây dựng component, hooks, áp dụng design patterns trong React. |
| `nextjs-expert` | Tối ưu SSR/SSG, App Router, tối ưu hóa hình ảnh và SEO trong Next.js. |
| `vue-expert` | Phát triển ứng dụng Vue.js với Composition API và Pinia. |
| `css-expert` | Viết CSS hiện đại, Tailwind, Animations, Responsive Design. |
| `react-performance` | Tối ưu tốc độ render, memoization, code-splitting cho React. |
| `accessibility-expert` | Đảm bảo website đạt chuẩn A11Y, hỗ trợ trình đọc màn hình. |

### 2. Backend & Database
| Tên Skill | Khi nào sử dụng |
| :--- | :--- |
| `nodejs-expert` | Xây dựng server-side, xử lý file, stream, tối ưu runtime. |
| `nestjs-expert` | Phát triển backend với kiến trúc Module, Controller, Provider theo chuẩn NestJS. |
| `database-expert` | Thiết kế Schema, tối ưu câu lệnh truy vấn (Query Optimization). |
| `postgres-expert` / `mongodb-expert` | Xử lý chuyên sâu cho cơ sở dữ liệu quan hệ (SQL) hoặc tài liệu (NoSQL). |
| `prisma-expert` | Quản lý ORM, migration và typesafe database access. |
| `rest-api-expert` | Thiết kế API theo chuẩn RESTful, xử lý status codes, documentation. |
| `auth-expert` | Triển khai JWT, OAuth2, bảo mật session và phân quyền (RBAC). |

### 3. Chất lượng mã nguồn (Code Quality)
| Tên Skill | Khi nào sử dụng |
| :--- | :--- |
| `typescript-expert` | Sử dụng Generic, Interface, Utility Types để code typesafe tuyệt đối. |
| `code-review` | Phân tích mã nguồn, tìm lỗi logic, đánh giá Clean Code & SOLID. |
| `refactoring-expert` | Cấu trúc lại code cũ, giảm độ phức tạp, tăng khả năng bảo trì. |
| `testing-expert` | Viết unit test, integration test, E2E test. |
| `jest-expert` / `vitest-expert` | Sử dụng các framework test chuyên biệt để đảm bảo code ổn định. |
| `playwright-expert` | Tự động hóa kiểm thử trình duyệt (E2E Testing). |

### 4. Công cụ & Quy trình (DevOps/Tools)
| Tên Skill | Khi nào sử dụng |
| :--- | :--- |
| `git-expert` | Xử lý commit, branch, merge/rebase và giải quyết xung đột (conflict). |
| `docker-expert` | Container hóa ứng dụng, viết Dockerfile và docker-compose. |
| `github-actions-expert` | Tự động hóa CI/CD, chạy test và deploy tự động. |
| `vite-expert` / `webpack-expert` | Cấu hình bộ build, tối ưu hóa bundle size và tốc độ phát triển. |
| `documentation-expert` | Tự động tạo README, tài liệu kỹ thuật (Markdown, JSDoc). |

---

## 💡 Cách gọi đúng & Kích hoạt

### 1. Chế độ Tự động (Auto-apply - Khuyên dùng)
Bạn không cần gọi tên skill chính xác. Chỉ cần ra lệnh dựa trên mục tiêu:
- **Yêu cầu:** "Thiết kế một trang Dashboard quản lý doanh thu"
- **Kết quả:** Tôi tự động áp dụng `ui-ux-pro-max` + `react-expert` + `css-expert`.

### 2. Chế độ Chỉ định (Direct Request)
Nếu bạn muốn tôi tập trung vào một kỹ năng cụ thể, bạn có thể nhắc đến nó trong câu lệnh:
- "Dùng skill **code-review** để kiểm tra file `AuthService.ts` này"
- "Áp dụng **ui-ux-pro-max** để gợi ý bảng màu cho web mỹ phẩm"
- "Refactor đoạn code này theo chuẩn **typescript-expert**"

### 3. Sử dụng Workflow nhanh (Slash Commands)
Dự án của bạn có 2 workflow cực mạnh đã được tích hợp:
- `/request`: Gọi quy trình Chuyên gia Full-Stack để giải quyết yêu cầu từ đầu đến cuối.
- `/ui-ux-pro-max`: Kích hoạt quy trình thiết kế chuyên sâu (Lập kế hoạch -> Chọn màu/font -> Implement).

---

## 🏁 Ghi chú quan trọng
Mọi skill đều tuân thủ các quy tắc cốt lõi trong thư mục `.agent/rules`:
1. **Sạch sẽ:** Luôn ưu tiên Clean Code & SOLID.
2. **An toàn:** Luôn kiểm tra bảo mật và hiệu năng.
3. **Hiện đại:** Luôn cập nhật các tính năng mới nhất của ngôn ngữ/framework.

Bạn muốn thử nghiệm skill nào ngay bây giờ? Hãy cho tôi một yêu cầu!
